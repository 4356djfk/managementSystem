<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.manage.managesystem.mapper.UserRoleMapper">

    <select id="selectRoleCodesByUserId" resultType="java.lang.String">
        select r.role_code
        from sys_role r
                 inner join sys_user_role ur on ur.role_id = r.id
        where ur.user_id = #{userId}
        order by r.role_code
    </select>

    <insert id="insert" parameterType="com.manage.managesystem.entity.UserRoleEntity">
        insert into sys_user_role (
            id, user_id, role_id, created_at
        ) values (
            #{id}, #{userId}, #{roleId}, #{createdAt}
        )
    </insert>

    <delete id="deleteByUserId">
        delete from sys_user_role where user_id = #{userId}
    </delete>

</mapper>
